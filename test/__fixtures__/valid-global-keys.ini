# INI file with only global keys (no sections)

# Application metadata
app_name=GlobalKeysTest
version=2.5.1
author=Test Author

# Configuration
debug=true
log_level=info

# Paths
root_dir=/var/app
data_dir=/var/app/data

# Multi-value global key
allowed_hosts=localhost 127.0.0.1 example.com
